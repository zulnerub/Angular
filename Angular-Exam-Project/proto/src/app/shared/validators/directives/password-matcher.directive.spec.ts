import { PasswordMatcherDirective } from './password-matcher.directive';

describe('PasswordMatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordMatcherDirective();
    expect(directive).toBeTruthy();
  });
});
